import { k } from "./engine.js";
import "./loader.js"; // This will run the content of the file, importing all assets

// rest of scenes code
k.scene("play", () => {
    // play scene code
});

k.go("play");